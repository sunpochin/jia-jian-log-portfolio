# 檔案用途：列出家健錄前端與通知代理可使用的環境變數範例，不包含任何正式機密。
# 所在層：repository root；供本機 `.env`、Vercel staging／production 設定時對照。
# 主要關聯：src/lib/supabase.ts、GoogleSignInButton、worker/ 與 scripts/check-care-ops-config.ts。

# 為什麼只留通用值：範例檔會隨公開 repo 流通，不能成為既有照護環境的設定索引。
VITE_SUPABASE_URL=https://your-project.supabase.co
# Prefer the publishable key; keep anon only for older Supabase projects.
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key
# VITE_SUPABASE_ANON_KEY=your-legacy-supabase-anon-key
VITE_GOOGLE_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# Google Sheets sync (optional)
# VITE_GOOGLE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/your-deployment-id/exec

# Google Identity Services uses this public Web Client ID to render the official
# button; the Supabase Google provider still verifies the ID token. Who's allowed
# to sign in is controlled by the `profiles` table in the database itself.

# scripts/seed-staging.ts、scripts/verify-staging-data.ts 專用——Service Role Key 會繞過所有 RLS，
# 只能本機手動執行時設定，絕對不要提交進 .env、也不要跟 production 的 SUPABASE_SERVICE_ROLE_KEY 共用。
# STAGING_SUPABASE_URL=https://your-staging-project.supabase.co
# STAGING_SUPABASE_SERVICE_ROLE_KEY=never-commit-service-role-key
